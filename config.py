from Tree import Vertex

# size of working field (rectangle with diagonal (0,0) and (WIDTH, HEIGHT))
WIDTH = 500
HEIGHT = 500

# list of lists [a, b, c], where a,b,c from R^2 - triangle vertices
obstacles = [[Vertex(20, 20), Vertex(120, 20), Vertex(70, 80)]]
